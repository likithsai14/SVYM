# Pagination Implementation for Admin Placements

## Tasks
- [x] Update backend `getPlacements.js` to support pagination (page, limit query params)
- [x] Update frontend `admin_placements.js` to handle pagination state and UI
- [x] Update `admin_placements.html` to add pagination container
- [x] Test pagination functionality with filters
- [x] Implement server-side filtering to work with pagination

## Details
- Default items per page: 10
- Pagination controls: Previous, Next, Page numbers
- Maintain filters across pages
- Server-side search and status filtering
